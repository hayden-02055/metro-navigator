# 부산 도시철도 역간 이동 경로 탐색 시스템

부산 도시철도 전 노선(1~4호선, 부산김해경전철, 동해선)을 **인접 리스트**로 표현하고,
**BFS**와 **다익스트라**로 두 가지 경로를 탐색하여 비교 출력하는 프로그램입니다.

- **BFS (deque / 0-1 BFS)** → 최소 환승 경로
- **다익스트라 (heapq)** → 최단 시간 경로

## 실행 방법

```bash
cd src
python3 busan_subway.py
```

실행하면 먼저 테스트 케이스 결과가 출력되고, 이어서 대화형 입력 모드로 진입합니다.

```
출발역> 남포
도착역> 수영
```

- 존재하지 않는 역을 입력하면 오류 메시지 후 다시 입력받습니다.
- 동명이역(`좌천`, `동래`)은 `좌천(동해선)`처럼 노선을 붙여 입력합니다.
- `q` 를 입력하면 종료합니다.

## 파일 구조

```
과제/
├── build_graph.py         # CSV → metro_data.py 생성기 (+ 검증)
├── src/
│   ├── busan_subway.py    # 메인 구현 (그래프 탐색 + 출력/입력)
│   └── metro_data.py      # 인접 리스트 데이터 (GRAPH, 자동 생성)
├── data/
│   └── busan_metro.csv    # 단일 진실 공급원 (line, order, station, transfer)
└── README.md              # 본 문서
```

> 데이터는 `data/busan_metro.csv` 가 단일 진실 공급원입니다. 데이터를 고칠 때는 CSV 를
> 수정한 뒤 `python3 build_graph.py` 로 `src/metro_data.py` 를 재생성합니다. 실행
> 프로그램(`busan_subway.py`)은 CSV 를 직접 읽지 않고 생성된 `GRAPH` 만 import 합니다.

### 데이터 재생성

```bash
python3 build_graph.py
```

`build_graph.py` 는 CSV 를 인접 리스트로 변환하면서 ① 노선별 역 수, ② 에지 대칭성,
③ 동명이역 충돌 해소, ④ 총 노드 수(146)를 검증하고, 하나라도 어긋나면 생성을
중단합니다. 표준 라이브러리(`csv`, `collections`)만 사용합니다.

## 인접 리스트 모델

```python
GRAPH = {
    "서면": [
        ("범내골", 2, "1호선"),
        ("부전",  2, "1호선"),
        ("가야",  2, "2호선"),
        ("전포",  2, "2호선"),
    ],
    ...
}
```

- **노드** = 역이름(문자열)
- **에지** = `(이웃역, 소요시간(분), 노선명)`, 일반 인접역 이동은 **2분**
- **환승역**(서면·연산·동래 등)은 **한 노드**에 여러 노선의 에지가 함께 모입니다.
  탐색 중 직전 주행 노선과 다른 노선의 에지로 이동하면 **환승**으로 보고 **3분**을 더합니다.
- 환승 표시가 없는 **동명이역**(`좌천`, `동래`)은 `좌천(동해선)` 처럼 노선을 붙여
  **서로 다른 노드**로 구분합니다. (`좌천`: 1호선/동해선, `동래`: 1·4호선 환승역과
  동해선 동래역은 공식 환승이 아니므로 분리)

## 클래스 구성

| 클래스 | 역할 |
|--------|------|
| `RouteResult` | 탐색 결과(경로/환승/시간)를 담는 값 객체 |
| `BusanMetro` | 인접 리스트(`GRAPH`) 보관 + `bfs()` / `dijkstra()` |
| `MetroApp` | 결과 출력(`print_result`) + 사용자 입력 루프(`interactive`) |

## 알고리즘

| 메서드 | 자료구조 | 목적 | 비용 우선순위 |
|--------|----------|------|----------------|
| `bfs` | `deque` (0-1 BFS) | 최소 환승 | (환승횟수, 시간) |
| `dijkstra` | `heapq` | 최단 시간 | (시간, 환승횟수) |

두 메서드 모두 `RouteResult` 를 반환하며, 경로가 없으면 `found == False` 입니다.
방문 체크는 `(역, 노선)` 단위로 하여 같은 역이라도 다른 노선으로 도달하는 경우를 구분합니다.

## 데이터 검증 결과

데이터는 공개 자료(부산교통공사·한국철도공사) 기준으로 정합성을 맞추었으며,
`build_graph.py` 생성 검증과 아래 경로 케이스를 통과했습니다.

| ID | 케이스 | 기대 결과 | 결과 |
|----|--------|----------|------|
| V2 | 남포 → 노포 | 1호선 직통·환승 0·수안~서동 미경유 | ✅ 24역 46분, 환승 0 |
| V3 | 수영 → 광안 | 1정거장 2분 | ✅ 2분, 환승 0 |
| V4 | 미남 → 안평 | 4호선 직통·14역 | ✅ 14역 26분, 환승 0 |
| V5 | 부전 → 태화강 | 동해선 직통·교대·신해운대·송정 경유 | ✅ 23역 44분, 환승 0 |
| V6 | 장산 → 대저 | 4호선 금련산·재송·센텀 미출현 | ✅ 환승 1회 (해당 역 미경유) |
| V7 | 사직 → 안락 | 거제 환승(3호선↔동해선) | ✅ 거제 환승, 환승 1회 13분 |

## 제약 조건

- 외부 라이브러리 미사용 (`heapq`, `collections`, `dataclasses` 표준 라이브러리만 사용)
- Python 3.8 이상
